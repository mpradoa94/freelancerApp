<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mpa.freelancer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- I changed it to test my view but we have to define the view that appears at lauch later -->
    <application
        android:name=".ParseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".register">

            <!-- </activity> -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login" />
        <activity android:name=".Profile" />
        <activity android:name=".EditProfile" />
        <!-- <activity android:name=".register"></activity> -->


        <!--
         <meta-data
            android:name="com.parse.APPLICATION_ID"
            android:value="vy6ZcwiJIdQmApaVslMdJXkeiK9xbNH7Ni35ATWQ" />
        <meta-data
            android:name="com.parse.CLIENT_KEY"
            android:value="KaNrovlikvnVHzJoXvcL98uqUQYe7wEYeDo9wQsX" />
        -->

        <activity android:name=".search" />
        <activity android:name=".EditSkills" />
        <activity
            android:name=".MenuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>